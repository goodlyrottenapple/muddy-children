\RightLabel{ $Id$ }
\AxiomC{$ \textsf{dirty}(n,J) \vdash \textsf{dirty}(n,J) $}
\RightLabel{ $Prem$ }
\AxiomC{$ \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \textsf{vision}(n) $}
\RightLabel{ $Prem$ }
\AxiomC{$ \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ \textsf{E}(n)^{k+1} \textsf{vision}(n) ; \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $C_L$ }
\UnaryInfC{$ \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ \textsf{dirty}(n,J) ; \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \textsf{dirty}(n,J) \wedge \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ \textsf{dirty}(n,J) \wedge \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \textsf{dirty}(n,J) \wedge \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}
\RightLabel{ $Prem$ }
\AxiomC{$ \textsf{dirty}(n,J) ; \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \{ j \} \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ (\textsf{dirty}(n,J) ; \textsf{vision}(n)) ; \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $A_L$ }
\UnaryInfC{$ \textsf{dirty}(n,J) ; \textsf{vision}(n) ; \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ \textsf{vision}(n) ; \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{dirty}(n,J) >> \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{dirty}(n,J) >> \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ \textsf{dirty}(n,J) ; \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ \textsf{dirty}(n,J) \wedge \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash D_j \rightarrow \bot >> \textsf{dirty}(n,J') $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \vdash \{ j \} (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}\RightLabel{ $Back\_forw\_K$ }
\UnaryInfC{$ \,\rotatebox[origin=c]{90}{$\{\rotatebox[origin=c]{-90}{$ j $}\}$}\, \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \vdash (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \{ j \} \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $Back\_forw\_K$ }
\UnaryInfC{$ \,\rotatebox[origin=c]{90}{$\{\rotatebox[origin=c]{-90}{$ j $}\}$}\, \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ \,\rotatebox[origin=c]{90}{$\{\rotatebox[origin=c]{-90}{$ j $}\}$}\, \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; \,\rotatebox[origin=c]{90}{$\{\rotatebox[origin=c]{-90}{$ j $}\}$}\, \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $K_mon_L$ }
\UnaryInfC{$ \,\rotatebox[origin=c]{90}{$\{\rotatebox[origin=c]{-90}{$ j $}\}$}\, (\Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; \Box_j \textsf{E}(n)^k \textsf{vision}(n)) \vdash ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $Back\_forw\_K2$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \{ j \} ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \{ j \} ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n)) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash D_j \rightarrow \bot >> \textsf{dirty}(n,J') $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $W <_L$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash \textsf{E}(n)^k \textsf{vision}(n) << \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, <)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash D_j \rightarrow \bot >> \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') ; \textsf{E}(n)^k \textsf{vision}(n) \vdash ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n) \vdash ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \{ j \} ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n))) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash D_j \rightarrow \bot >> \textsf{dirty}(n,J') $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash D_j \rightarrow \bot >> \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\textsf{E}_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) ; D_j \rightarrow \bot \vdash \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ (D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) ; D_j \rightarrow \bot \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\textsf{E}_L$ }
\UnaryInfC{$ ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) ; D_j \rightarrow \bot) ; D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $A_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) ; D_j \rightarrow \bot ; D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\textsf{E}_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot ; D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, <)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) << (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $A_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot ; D_j \rightarrow \bot) ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) << (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, <)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; D_j \rightarrow \bot \vdash (\textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) << (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) << (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}\RightLabel{ $C_L$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash (\textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) << (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) << (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') $}\RightLabel{ $(;, <)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) << (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, <)$ }
\UnaryInfC{$ (D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $A_L$ }
\UnaryInfC{$ D_j \rightarrow \bot ; (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash D_j \rightarrow \bot >> \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') ; (D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n) \vdash (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n)) \vdash (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n))) \vdash \{ j \} (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n))) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n)) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Prem$ }
\AxiomC{$ \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) \vdash [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) \vdash D_j \rightarrow \bot >> [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n) \vdash (D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \{ j \} (D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \Box_j ((D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}\RightLabel{ $W >_R$ }
\UnaryInfC{$ \textsf{father}(n) \vdash D_j \rightarrow \bot >> D_j \rightarrow \bot $}\RightLabel{ $(;, >)$ }
\UnaryInfC{$ D_j \rightarrow \bot ; \textsf{father}(n) \vdash D_j \rightarrow \bot $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \wedge \textsf{father}(n) \vdash D_j \rightarrow \bot $}
\RightLabel{ $Prem$ }
\AxiomC{$ [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) >> [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) \vdash \{ j \} (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}
\RightLabel{ $Id$ }
\AxiomC{$ D_j \vdash D_j $}
\RightLabel{ $\bot_L$ }
\AxiomC{$ \bot \vdash I $}\RightLabel{ $\bot_R$ }
\UnaryInfC{$ \bot \vdash \bot $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ D_j \rightarrow \bot \vdash D_j >> \bot $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ D_j \rightarrow \bot \vdash D_j \rightarrow \bot $}
\RightLabel{ $Id$ }
\AxiomC{$ \textsf{father}(n) \vdash \textsf{father}(n) $}\RightLabel{ $\wedge_R$ }
\BinaryInfC{$ D_j \rightarrow \bot ; \textsf{father}(n) \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) $}\RightLabel{ $\wedge_L$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \wedge \textsf{father}(n) \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) $}
\RightLabel{ $Prem$ }
\AxiomC{$ [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_L$ }
\BinaryInfC{$ (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) >> [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $\rightarrow_R$ }
\UnaryInfC{$ (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} \vdash (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_L$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) \vdash \{ j \} (D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'} $}\RightLabel{ $FboxK_R$ }
\UnaryInfC{$ \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J') \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge ((D_j \rightarrow \bot) \rightarrow \textsf{E}(n)^k \textsf{vision}(n))) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \Box_j (((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \textsf{E}(n)^k \textsf{vision}(n)) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \Box_j ((D_j \rightarrow \bot) \rightarrow \textsf{dirty}(n,J')) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \textsf{dirty}(n,J) \wedge \textsf{vision}(n) \wedge \Box_j \textsf{E}(n)^k \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\RightLabel{ $Cut$ }
\BinaryInfC{$ \textsf{dirty}(n,J) \wedge \textsf{E}(n)^{k+1} \textsf{vision}(n) \vdash \Box_j ((D_j \rightarrow \bot) \wedge \textsf{father}(n) \rightarrow [ \textsf{father}_n ] [ \textsf{no}_n ]^k \Box_{j'} D_{j'}) $}\DisplayProof